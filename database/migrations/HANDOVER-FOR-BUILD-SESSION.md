# NRG SolarConnect — Handover to Build Session

This replaces any earlier handover you were given. The design session has moved on a lot since then — grounded against ~30 real NRG documents and spreadsheets, not just the original vision doc.

## What NRG SolarConnect is (unchanged, frozen)

A **Project Intelligence Repository** for NRG Technologists (solar EPC): store every project document, extract structured data with AI, connect it across documents, and generate measurable intelligence — not a CRM/ERP/PM tool. Stack: Next.js frontend → Supabase (Postgres) → Google Drive (document storage) → OpenAI (extraction). Full detail, principles, and the reasoning behind every table: `docs/nrg-solarconnect-handover.md` in the `nrg-report-service` repo (send that file over too, or pull it if you have repo access).

## What to do right now

1. Run `0002_core_schema.sql` through `0021_dc_ac_ratio.sql`, then `0022_project_costs_suggested_match.sql`, then `0023_bom_category_cost_and_material_merge.sql`, then `0024_cost_benchmark_by_project_size.sql`, then `0025_transport_cost_and_project_margin.sql`, then `0026_dc_ac_lower_bound_and_document_exemptions.sql`, then `0027_purchase_receipt_invoice_reconciliation.sql`, then `0028_pipeline_projects_and_margin_estimate.sql`, then `0029_project_duplicate_repair.sql`, then `0030_document_reassignment.sql`, then `0031_material_capacity_band_matching.sql`, then `0032_lost_projects_and_retrospective_margin.sql`, then `0033_payment_receipt_generation.sql`, then `0034_pipeline_bom_rate_and_estimate_overrides.sql`, then `0035_multi_tenant_foundation.sql`, then `0036_sales_quotes_and_followup.sql`, then `0037_service_desk.sql` (all in this directory) against your Supabase project, in that order, after your existing `0001_init.sql`. `0037` is the Service Desk module (tickets + AMC), the largest and most business-critical of the five live Apps Script tools it replaces. **The single biggest structural fix**: the live system ran three disagreeing customer universes (Service Desk's own synced CUSTOMERS tab, Sales' Clients tab, and NRG SolarConnect's own `customers`/`projects`) and three independent warranty calculators because of it. Here there's exactly one `customers` table and one `project_warranty_status` view, computed live off `commissioning_reports.commissioning_date` (already exists, Section 37) using the one rule that actually gated real business logic in the live system (1yr if GEDA-sourced, else 5yr) — a project with no confirmed commissioning date reports warranty as genuinely unknown, never guesses. `customers.merged_into_customer_id` added (same pattern as materials/projects, Sections 60/69) since the live multi-source sync produces real duplicates. A ticket/AMC can exist without a project (`project_id` nullable throughout) since AMC serves third-party, non-NRG-installed systems too. `service_tickets`/`ticket_timeline`/`ticket_documents` (photos as ordinary `documents` rows via a join table — the live system had two different, inconsistently-anchored Drive folder conventions for tickets vs. AMC; here it's one), `service_assignment_rules` (a real editable table replacing a static map duplicated in two files live), `service_quotes`/`service_quote_items` (Visit/WiFi/Parts charge quotes against a ticket), and the full AMC set — `amc_rates` (tenant-scoped rate card, same reasoning as `quote_kw_band_rates`, 0036), `amc_quotes`, `amc_contracts`, `amc_visits`, `amc_visit_documents`. **`amc_contracts` deliberately has no stored status column** — the live AMC_REGISTER never transitioned a contract to Expired once its end date passed, a confirmed-dead workflow; `amc_contract_status` computes active/expired/cancelled live instead, so there's nothing to go stale. Its `due_for_renewal` flag replaces the live daily-9am renewal trigger, which — despite a real schedule and a UI badge — only ever wrote to an execution log and notified nobody; now it's a real, trustworthy query. The Commissioning Readings modal in the live ticket UI silently discarded everything typed into it (confirmed dead) — fixed not with a new table but by wiring a commissioning-type ticket to the `commissioning_reports` table that already exists. Gated behind `module_entitlements` (0035) `module_key = 'service'`. **Not built**: WhatsApp/email send logic, real payment-gateway integration (stays staff-attested, matching `payment_receipts` elsewhere), and RLS (this migration shapes the tables for "an engineer sees their own tickets, Owner/Service Head sees all" — doesn't enforce it yet; the live system's permission checks were confirmed almost entirely client-side, "Admin only" comments with zero server enforcement — fixing that is what real RLS is for). `0036` is the first Sales/Service module rebuilt from a full read of the live Apps Script source it replaces (Quote Generator + Sales Follow-up — two separately-deployed projects that share one live spreadsheet with no API boundary, `docs/system-understanding-2026-08-16.md`), built as one migration since that coupling is real. Adds `leads` (raw inbound inquiries, pre-quote), `customer_pipeline` (CRM state — temperature/follow_up_status/stage/contact counts — deliberately its own table, not columns on `customers`, so a tenant without the `sales_followup` entitlement carries none of this), `quotes` (one row per quote, replacing BOTH the live "Quotes" and "Historical" tabs with one shape — no more two projects reading the same legacy data two different fragile ways), `quote_extras` (normalized, not the live payload's JSON array), `quote_panel_rates`/`quote_kw_band_rates` (a real rate table replacing a spreadsheet scanned by row/column position for wattage bands and brand rows — fragile-but-working today, structurally safer as normal columns), `customer_activity_log` (one unified log — the live system has two disconnected logging paths today, a Sales Follow-up dashboard tap and a Quote Generator "Share on WhatsApp" tap that never touch the same counter), and `sales_targets`. Two views — `customer_quote_rollup` and `salesperson_daily_activity` — replace stored, drift-prone columns (`Times Quoted`/`Latest Quote URL` on the live Clients tab) with live computation, same `bom_category_cost` (0023) discipline. **Three real bugs fixed, not carried forward**: (1) the live CEO-discount path read Quote Generator's columns by hardcoded position (`row[54]` etc.) — the single most dangerous latent bug found reading the source; a real table with named columns makes it structurally impossible. (2) Historical/Quotes unification, above. (3) Deal Status (per-quote)/Temperature (per-customer)/Follow-up Status (per-customer) were three overlapping fields the live code never reconciled — quoting a client silently reset their temperature and blanked their last-follow-up date; kept as three real distinct concepts here (`quotes.quote_time_temperature` vs. `customer_pipeline.temperature`) but `quotes` deliberately never auto-writes `customer_pipeline` — a customer's CRM state changes only from a visible, attributable action. CEO-discount PDF regeneration reuses `documents.supersedes_document_id` (Section 19, already exists) rather than inventing new versioning — the prior PDF's link is never touched. Gated behind two separate `module_entitlements` (0035) rows, `sales_quotes` and `sales_followup`, even though NRG uses both together. **Not built**: OCR/WhatsApp/email send logic (application-layer, same deep-link pattern as elsewhere), RLS (still open — this migration only shapes the tables so Section 68's row-level rule can finally be written correctly), and the ordinary per-kW discount's server-side cap (currently client-JS-only in the live system — the column is here, the enforcement belongs with the RLS design). `0035` is a deliberate, owner-requested architecture change: get NRG live single-tenant as fast as possible, but don't close the door on selling this to other solar EPCs later, module by module. Adds `organizations` (the tenant table, seeded with one row for NRG itself) and `module_entitlements` (organization × module_key × tier `basic`/`advanced` — a missing row means that module is off for that tenant; `module_key` is free text like `bom_items.category`, so the Sales/Service modules about to be built each just need one more row here, never a schema change; NRG is seeded with every module at `advanced`). Tenant scoping is added only to the schema's five true anchor tables — `employees`/`partners`/`customers`/`materials`/`projects` — not every table; everything else already reaches one of these five via an existing foreign key, so a child row's tenant is derived through that FK, never a second independently-set copy of the same fact. `materials`'s `unique(category, canonical_name)` (0002) becomes `unique(organization_id, category, canonical_name)` since a category name like "DC Cable 4 sq mm" is ordinary for any solar EPC, not something that should collide across tenants. **Deliberately not built**: RLS policies that actually enforce this isolation (still open, same item 4 as always — this migration makes that policy correct to write, doesn't write it), per-tenant branding, signup flow, billing, or an admin console for other companies — none of that has a real second customer to design against yet. `0034` needs no new column for two of its three asks: Rate being editable alongside Quantity on a pipeline BOM reuses `bom_items.planned_rate` (already existed, Section 19, same `extraction_status`/`confirmed_by`/`confirmed_at` correction machinery as `planned_quantity`) — purely a UI gap that the mockup only ever exposed a quantity input, never a rate one; and adding a missing BOM line item (the real example that surfaced this: Lightning Arrestor/MC4 Connectors/Earthing Pits/Misc hardware were absent from a standardized starting BOM entirely) is just inserting an ordinary new `bom_items` row, which was always possible, again just missing a "+ Add Line Item" UI affordance. The one real schema addition: `boms.estimated_transportation_cost`/`estimated_fabricator_cost`/`estimated_electrical_contractor_cost` (nullable, header-level, same pattern as `order_value`/`kw_capacity`) let Owner override the size-band standard average (0024) with a real number for *this* deal — an actual fabricator quote, a known transporter rate for this route — rather than always falling back to the generic benchmark. `pipeline_project_margin_estimate` (0028) is recreated to `coalesce(boms.estimated_*_cost, size-band standard)` for each of the three, and its `has_*_benchmark` flags now also count a manual override as "has a number," same reasoning as before (Section 64/68) — a missing cost must never silently read as zero. Concrete, illustrative result worth knowing: reworking Bansal Ceramics' pipeline BOM with the real qty × rate on all 13 (9 original + 4 previously-missing) line items flips its estimated margin from a healthy +11.92% to an actual **loss** of −3.08% at the quoted price — the standardized estimate wasn't wrong about what it counted, it was silently incomplete, and that alone was enough to flip a take/leave decision. Also fixed in the mockup only, no migration: Service's reduced pipeline-BOM view now actually drops the Rate/Line Cost columns (quantities/units stay visible) — Section 73 had stated this boundary but the mockup hadn't yet implemented it at the column level, only hid the whole BOM section for Sales. **One more mockup-only change, no migration, flagged for the real build's document templates**: every generated-document sample (DC, Material Return Note, BOM, Commissioning Report, PO, Payment Receipt — Section 76) now uses NRG's real letterhead instead of a placeholder — checked directly against real NRG documents the owner shared (Quote/Proposal, Debit Note, Delivery Challan, Tally Sales Invoice). NRG already has a genuinely professional template (navy header, logo, "Quality | Commitment | Value" footer motto on the Quote/Debit Note) that simply isn't applied to every document type today (the real Delivery Challan and Sales Invoice have no letterhead at all) — the fix is consistency, not a new design. Real registered address/GSTIN now used throughout (`989/6, G.I.D.C. Makarpura, Vadodara, Gujarat 390010` / `24AABCN8993K1ZV`, confirmed off three separate real documents) in place of the placeholder GSTIN the mockup had. The logo mark in the mockup is an SVG approximation standing in for the actual logo file provided — **the real build should embed that actual asset**, not this stand-in. No schema involved: every field these templates print already comes from existing tables (Section 37/74), this is purely which template renders them. `0033` adds `payment_receipt_documents` — a one-click, auto-populated, signed payment receipt, same generated-document pattern as `delivery_challans`/`commissioning_reports` (0010): `receipt_number` from `next_document_number()` (reused, no schema change, `'PR'` is just a new series), fields pulled from `payment_receipts` (Section 32 — amount/payment_date/payment_method/payer_name/reference_number/what it's towards, all already there) at generation time, not re-derived live, since a signed receipt shouldn't silently change if the underlying record is edited later. `unique(payment_receipt_id)` — one receipt per confirmed payment, never two; a reprint reuses the same row/number. **Gate generation on `payment_receipts.match_status = 'confirmed'`** — an unconfirmed AI match has no business becoming a signed client-facing document. **Also flagging a UI-only correction with no migration attached**: Section 73 refines the pipeline-project access rule from 0028/0032 — pricing there is **Owner-only**, not Owner/Sales Head (that pairing was always the separate row-visibility rule, Section 68, not a pricing-visibility one); Sales sees only its own uploaded documents (never the BOM), Service sees the editable BOM plus its own uploads (never pricing). No schema needed for the BOM-editing half either — `bom_items.extraction_status = 'corrected'` already covers it, same as any other BOM edit; a pipeline BOM's `bom_category_cost`/`bom_panel_vs_rest_cost` recompute live from whatever `planned_quantity` currently holds. `0032` adds `'lost'` to `projects.status` (how a pipeline project leaves the active In Talks list — a status, not a delete) plus `lost_amount`/`lost_recorded_by`/`lost_at` (optional — sales enters the amount only if they know what the deal actually went for elsewhere) and `lost_project_analysis`, which reuses `pipeline_project_margin_estimate` (0028) wholesale to compare `lost_amount` against NRG's own size-band-benchmark cost estimate — a direct, real answer to "should we have matched that price," only shown when `lost_amount` is actually known. Same Owner/Sales Head pricing boundary as 0028's correction: sales can record the amount, doesn't see the analysis drawn from it. `0031` adds `materials.rated_capacity_min_kw`/`rated_capacity_max_kw` (both null except for genuinely band-rated categories like ACDB/DCDB) so a required capacity (e.g. a BOM/PO's "30kW ACDB") can be matched against a vendor's real ranged product ("ACDB 25-40kW") by range containment, not exact-value text matching — `aliases` only ever handled spelling variants of the same number, not a value against a range that covers it. No new view — a plain `between` range query at BOM/PO creation (suggest the real banded material instead of inviting a phantom exact-value row) and as AI-extraction's fallback match when a receiving document has no `purchase_order_item_id` (0027) to anchor to yet. `0030` adds `documents.reassigned_from_project_id`/`reassigned_by`/`reassigned_at`/`reassignment_reason` for moving a misfiled document to the right project — same "audit the correction, don't silently overwrite" pattern as everything else. **Confirmed real restriction to carry into the roles/permissions design**: only Owner or Purchase can reassign a document, nobody else. `0029` adds `projects.merged_into_project_id` — same pattern as `materials.merged_into_material_id` (0023) exactly, for the same real risk now confirmed on projects: Sales, Service, and whoever processes an uploaded document can each independently create a project row for what's actually one real project, with no way to see what already exists. Prevention is app-level (fuzzy-match the typed customer name/site address against `customers.name`/`aliases` and existing `projects.site_address` before creating anything — no schema needed for that half); this column is purely the repair path once a duplicate already happened — re-point every `project_id` foreign key from the duplicate to the survivor, merge over whatever data only the duplicate had, set this column as the audit trail. **Also confirms + New Project needed to exist at all** — it was entirely missing from the mockup; one form now covers both an ordinary active project and an in-talks pipeline one (0028), Status chosen up front rather than two separate flows, since it's the same row either way. `0028` adds `'in_talks'` to `projects.status` (a prospective project — same row, same documents, same BOM as a won one, just no confirmed order yet; graduates to `'active'` in place) and `pipeline_project_margin_estimate`, which reuses `project_size_band`/`project_confirmed_bom`/`bom_panel_vs_rest_cost`/`size_band_standard_cost` wholesale to price a not-yet-won BOM at real current material rates plus the size-band standard for Transportation/Fabricator/Electrical Contractor (no real bills exist yet for an unconfirmed order), compared against `boms.order_value` — the manual "should we take this order" exercise NRG does by hand today. `has_*_benchmark` flags whenever a size band has no standard cost data point yet, so a thin estimate is never silently presented as a solid one. **Flagging a real access-control requirement for whenever the roles/permissions/RLS design (item 4, still open) happens**: pipeline projects need row-level visibility — a salesperson sees only their own (`projects.sales_person_id`), plus the Owner and a named Sales Head see all of them, via `employees.role` (not hardcoded employee IDs). Not built now, deliberately — this is the first genuinely row-level rule in the whole schema, everything else so far has been coarse role-gating. **Corrected same round, explicit rule**: Sales/Service must never see pricing/₹-per-watt anywhere, including inside a pipeline project they can otherwise see (even their own) — the pipeline screen's cost/margin figures need the exact same Owner-only gate as the BOM tab's `.cost-col` (Section 39), independent of the row-level "whose prospect is this" rule above; the two are separate axes, don't conflate them into one toggle in the real build. `0027` adds `material_transactions.invoice_document_id`/`invoice_match_status`/`invoiced_quantity`/`quantity_variance` (generated) so a PO's inward delivery challan and its eventual invoice reconcile into **one** `'purchased'` row instead of each independently creating its own — real risk flagged directly: a 100m order, a 100-120m delivery challan (bundle-length variance), then a 110m invoice, previously could credit stock 100+110=210m for a shipment that actually put 110m on the shelf. Whichever document is extracted first creates the row; the second matches via `purchase_order_item_id` (or vendor+material+date proximity with no PO) and reconciles into the same row — same AI-suggests/human-confirms pattern as `payment_receipts.match_status` elsewhere. `quantity` stays exactly what was physically received, permanently; `invoiced_quantity` is a separate column, never overwriting it. `material_stock`/`material_purchase_running_avg` re-created to use `coalesce(invoiced_quantity, quantity)`. New `pending_invoice_purchases` report falls out for free — material in stock with no invoice reconciled yet, a real AP gap this schema never surfaced before. `0026` does three things: adds `undersized` to `bom_capacity_check` (only `oversized` existed before — confirmed acceptable DC:AC band is 0.9-1.4, not just "under 1.4"); adds `project_document_exemptions` (project_id, document_type, a **required** `reason`, who/when) so the Required Documents Gap query can close an item that genuinely doesn't apply to a specific project (e.g. Board Resolution for an individual applicant) instead of leaving it open forever with no way to explain why — the gap logic now closes an item if either a real `documents` row exists or an exemption does; and adds `project_total_cost_per_watt`/`size_band_total_standard_cost` for a "Cost Above Panel Cost" figure on the size-band benchmark (Section 63), same non-panel-total concept as `bom_panel_vs_rest_cost` (0023) but carrying `size_band` and reusing `margin_data_complete` (0025) to exclude incomplete projects from the average. Also a UI-only fix worth knowing about: the Generated Documents report's rows were never actually clickable despite its own caption promising it — now routes to the real BOM tab / paper-doc sample per document type, and the Capacity Consistency Check report's single "Flag" column (which conflated two independent checks — government-document kW agreement and DC:AC sizing — into one signal) is now two separate columns, since a project can fail either one without the other. `0025` fixes a real bug in `0024` (transport bills were silently folded into "Installation" instead of their own line) and confirms the exact, complete list of what sits outside the BOM's own actual cost: **Transportation, Fabricator, and Electrical Contractor — three separate lines, not one blended "Installation."** `project_transportation_cost`, `project_fabricator_cost` (`cost_type = 'fabrication'`), `project_electrical_contractor_cost` (reads the existing `cost_type = 'installation_subcontract'` value — worth renaming that stored value to `'electrical_contractor'` outright when this gets built, so the label and the data finally match), and a `project_other_project_cost` catch-all (`'vendor_bill'`/`'other'` — kept internally so total cost is never silently short by whatever lands here, not surfaced as a named line since it's confirmed rare) replace `0024`'s single `project_installation_cost`. `project_cost_per_watt_benchmark` re-created to union all four. Also finally defines Margin/Profitability, deferred since Section 37: `project_margin` gives `margin_quoted`/`margin_pct_quoted` (against `boms.order_value`) and `margin_invoiced`/`margin_pct_invoiced` (against `project_invoiced_revenue`, confirmed `tax_invoice` rows) — Total Project Cost = the BOM's own actual cost + Transportation + Fabricator + Electrical Contractor + Other, never material alone. `boms.actual_total_cost` was already computed off actually-dispatched-minus-returned quantity at real weighted-average purchase rate (`bom_item_variance.actual_cost`, 0005) — no change needed there, margin was already inheriting the right number. New and important: `project_margin_completeness`/`project_margin.margin_data_complete` — a missing cost input (an undispatched/uncosted BOM line, zero captured bills in any of the three named cost types) must never silently read as zero in a profit calculation, since that understates cost and overstates margin in the direction that flatters the business; the UI must show margin in red/amber with exactly what's missing named whenever `margin_data_complete` is false, never hide it, and the same per-project incompleteness should exclude that project from `size_band_standard_cost`'s average for whichever column is missing, rather than silently pulling the average down. Both owner-only, same boundary as `0024`. Margin/Transportation/Fabricator/Electrical Contractor belong directly on the project's own BOM tab, alongside `0023`'s Cost per Watt panel — confirmed, not a separate screen. `0024` adds a cross-project, owner-only cost benchmark report (same access boundary as Project Margin, Section 39 — nobody else sees it): `project_confirmed_bom` (latest `extraction_status = 'confirmed'` BOM per project — an unreviewed BOM shouldn't feed a company-wide number), `project_size_band` (computed, not stored — buckets `kw_capacity` into 15-25/25-50/50-100/100-200/200-500/500+ kW slabs plus an "Under 15 kW" catch-all), `project_installation_cost` (rolls up `project_costs` where `assignment_status = 'assigned'` into Rs/Watt — labour/installation cost, missing from 0023's material-only view), `project_cost_per_watt_benchmark` (one row per project per category, `bom_category_cost` unioned with the new installation row — the direct report source, one tab per size band, projects as columns, category as rows), and `size_band_standard_cost` (avg/min/max/count of Rs/Watt per size band and category — the actual "what to charge a future project this size" number). Still not the deferred margin/profitability view — no revenue involved anywhere in 0024, purely a cost-side benchmark. `0023` adds two views — `bom_category_cost` (Rs/Watt per BOM category — GI Structure, ACDB/DCDB, DC Cable, AC Cable, etc. — from `bom_items.planned_quantity × planned_rate` divided by `boms.kw_capacity`) and `bom_panel_vs_rest_cost` (the same figure split into `'Solar Panels'` vs. every other category summed, so a panel-brand swap's cost impact is isolated from everything else) — plus `materials.merged_into_material_id`, a nullable self-FK for repairing a material that got entered twice under different names (e.g. "Growatt 50kW" vs. "Solar Lian 50kW" for the same product): the merge itself is an app-level operation that re-points every `material_id` foreign key to the surviving row and folds the duplicate's name into the survivor's `aliases`, this column is just the audit trail marking the duplicate as merged. Neither view is the deferred margin/profitability report mentioned below — there's no revenue involved, this is a pure BOM cost breakdown. `0020` adds the `stock_adjustment` movement type + `stock_adjustments` table + `stock_adjustment_summary` view (monthly physical count vs. book stock, with a ₹ value on the variance) and, in the same `create or replace view`, **fixes a real bug**: `material_stock` never actually subtracted `sold_direct` despite 0008's own comment claiming it did — every direct sale has been overstating stock since 0008 shipped. `0021` adds `boms.inverter_capacity_kw` and the `bom_capacity_check` view (`dc_ac_ratio`, flagged past 1.4). `0022` adds a `'suggested'` state to `project_costs.assignment_status` plus `ai_confidence`, so a multi-project fabricator/electrician bill (each line naming its own project) can auto-suggest a match instead of starting every line unassigned. `0018` adds `bank_statement_transactions` (uploaded bank statement lines, AI-extracted and matched against `tally_ledger_entries` the same way `payment_receipts` already is) and the `bank_reconciliation_gaps` view (everything unmatched/discrepant on either side — the actual "check now" screen for accounts). `0019` adds `vendor_inquiries` — a log of WhatsApp (or phone/email) inquiries sent to vendors, one row per vendor per send, no delivery/read tracking since `wa.me` click-to-chat gives none. `0015` adds `vendor_quote_items.estimated_transport_charge`/`estimated_loading_unloading_charge`, `quote_comparison_requests.total_amount` (the ₹25,000 owner-approval threshold), and `partners.supplies_categories`. `0016` recreates `material_shortfall` with a new `order_now` column (the real In Stock + Order Placed + Min Level − On Hand Orders formula), adds `boms.owner_approved_by`/`owner_approved_at` for a second sign-off before a generated BOM drives ordering, and recreates `material_requirement` to gate on it. `0017` adds `purchase_orders.po_number`/`created_by` and `commissioning_reports.cr_number` (both were missing a document number, and POs were missing a creator entirely), plus the `generated_documents_log` view — a `union all` across `delivery_challans`/`purchase_orders`/`commissioning_reports`/`boms` for a single "everything we've generated" list. `0002` creates: `employees`, `partners`, `customers`, `materials`, `customer_contacts`, `projects`, `documents`, `project_external_references`, `drive_folder_import_candidates`, `boms`, `bom_items`, `material_transactions`, and the `bom_item_variance` view. `0003` adds `purchase_orders`/`purchase_order_items`, a few columns on `materials`/`material_transactions`, and four inventory views (`material_stock`, `material_open_orders`, `material_requirement`, `material_shortfall`). `0004` adds `vendor_quotes`/`vendor_quote_items`, `reorder_alerts`, and two more views (`material_last_purchase`, `material_quote_comparison`). `0005` fixes `bom_item_variance.actual_cost` with weighted-average cost attribution. `0006` adds `financial_obligations`, `sales_invoices`/`sales_invoice_items`, `payment_receipts`, `tally_ledger_entries`, `project_milestones`, and four views. `0007` adds `boms.origin`/`generation_inputs`, `tally_ledger_entries.sync_source`, `stock_statements`/`stock_statement_items`, point-in-time functions, `materials.stock_group`. `0008` adds `sold_direct` movement and nullable `sales_invoices.project_id`/`customer_id` for direct sales. `0009` makes `documents.project_id` nullable, adds `electrical_test_records`, and DCR/NDCR/batch-ref/replacement columns on `material_transactions`. `0010` adds `document_number_sequences`/`next_document_number()`, `delivery_challans`, `commissioning_reports`. `0011` adds `project_costs`. `0012` adds `customers.aliases` and fixes the `project_external_references.reference_type` check-constraint bug. `0013` adds `vendor_quote_items.includes_transport`/`includes_loading_unloading`/`notes`, `materials.make` (with a widened uniqueness constraint), `bom_item_variance.material_id`, the `material_consumption_benchmark` view, and a third `boms.origin` value (`'recommendation_engine'`). `0014` adds `partners.contact_person`, the `quote_comparison_requests` approval-gate table plus `vendor_quotes.comparison_request_id`, and fixes `stock_statement_summary` to expose `materials.make` (added in 0013, after this view was first written).
2. That's now the **full core schema** for everything designed so far, including the full `document_type` taxonomy (Section 36), the BOM Recommendation Engine (Section 40), and the quote-comparison approval workflow (Section 41). What's still open: the document processing workflow itself, user roles/permissions and RLS, Marketing module, PO generation (confirmed not critical, deprioritized), and margin/profitability reporting (explicitly deferred — cost capture exists via `project_costs` + `bom_item_variance.actual_cost`, but no view combining them into a margin figure yet, on purpose). Don't build UI/business logic against tables that don't exist yet.
3. `documents.document_type` values are now a settled reference list, not a guess — see Section 36 for the full list grouped by category (sales & compliance, GEDA/CEIG/DISCOM chain, engineering drawings, inventory/material movement, financial reconciliation, KYC/compliance checklist, visual) and the extraction-field mapping table showing what each type feeds downstream.

## Key decisions worth knowing before you build on top of this

- **`bom_items.category` and `documents.document_type` are free text, not enums.** NRG's real category/document-type lists are long and still growing (full list now in Section 36, sourced from the handover doc's Sections 22-25) — don't hardcode a fixed set in the DB layer; keep it as an app-level reference list.
- **`material_transactions` is one shared ledger** for both BOM variance tracking (project-level) and inventory (warehouse-level) — `purchased` rows with `project_id null` are warehouse inflow; `issued_to_site`/`returned_to_warehouse` rows are project-level movement. Don't build a separate inventory table.
- **`Projects` is one table for both business lines** (residential-subsidy and commercial/industrial), distinguished by `project_type`, not two parallel schemas.
- **A project can have multiple external reference numbers** (`project_external_references`) — NRG's own internal ref, GEDA registration number, DISCOM consumer number, DISCOM SR No., etc. Never assume one `reference_number` field is enough. `reference_type` is genuinely free text as of 0012 (a real bug in 0002 had it hard-constrained to five values, contradicting its own design intent) — don't reintroduce a check constraint here.
- **Google Drive folder linking**: `projects.google_drive_folder_id` is the durable link — never derive it from folder name (real folder names follow at least 5 inconsistent patterns). New projects should auto-create their Drive folder via a service account. NRG's ~300 existing project folders need a human-reviewed import (`drive_folder_import_candidates`) — there are confirmed duplicate folders for the same project across two existing Drive locations, so this can't be a blind automatic match.
- **Never store secrets in a plain column** — monitoring-portal credentials, API keys, etc. go through Supabase Vault or equivalent, not a `text` field on `projects`.
- **No historical material-transaction data exists to import.** NRG confirmed there's no separate dispatch/purchase log — the BOM workbook's single "Actual Qty Used"/"Actual Rate" columns per line are the only record. When backfilling a historical project, create **one synthetic `material_transactions` row per `bom_items` row** (`movement_type = 'issued_to_site'`, quantity/rate from those Actual columns), with `source_document_id` set to the **same document as `boms.source_document_id`** (not a separate Delivery Challan). That's the signal that distinguishes "backfilled aggregate" from real per-dispatch data later — no extra column needed. Don't leave historical rows with zero transactions; `bom_item_variance` would then show a false "0 used" instead of "not tracked at this granularity." Historical projects therefore only ever support Material/Cost Variance, not Excess/Short Purchase or Unreturned Material breakdowns — those need sent/received/returned as separate numbers that were never recorded pre-go-live.
- **Inventory shortfall is a live query, not a batch job.** `material_shortfall` (0003) already ties together current stock, open PO quantity, and outstanding demand from every active project's `bom_items`. The moment a new project's BOM gets extracted, its `planned_quantity` rows flow straight into that view — there's no separate "check inventory" step to build, just read the view. `materials.reorder_level` is the one manually-entered field (business judgment, not AI-extracted) driving the general low-stock alert.
- **Reorder reminders need an app-layer scheduled job — the DB only tracks state, it doesn't send anything.** `reorder_alerts` (0004) exists so a daily job can check `material_stock.current_stock <= materials.reorder_level`, and only create a new alert (and fire an email/WhatsApp/push notification — build that part) when no `open`/`acknowledged` alert already exists for that material, so the same shortage doesn't nag every run. `materials.reorder_responsible_employee_id` is who to notify; fall back to a purchase-role broadcast if it's null.
- **Quote comparison is one view, not a manual lookup.** `material_quote_comparison` (0004) joins every stored `vendor_quote_items` row to `material_last_purchase`, so the reorder screen can show every quote's rate next to what was last actually paid, delta pre-computed, cheapest first.
- **Costing uses weighted-average, priced as of each issue's date** (0005), not FIFO and not a single "current average" — the latter would make a completed project's reported cost silently change every time a new purchase happens. `material_transaction_cost` handles both real per-dispatch issues (rate looked up from purchase history) and Section 28's legacy backfilled rows (rate taken directly from the row itself, since it already carries the workbook's historical Actual Rate and there may be no purchase history to look up at all for an old project). **This is not a second accounting ledger** — it's a view computed over the same `material_transactions` rows Tally also reflects, used only for BOM cost variance. Never label `actual_cost`/`cost_variance` in the UI as an accounting/valuation figure — Tally stays the sole book of record for that.
- **Never trust which Drive folder a document was uploaded into** to determine its project — a person will misfile things. AI content classification (client name, consumer number, project reference) determines the project match regardless of folder; flag a mismatch rather than trusting placement. Same principle Section 26 already established for `document_type`, now applies to project linking too.
- **A bulk purchase split across many projects needs no special handling** — one warehouse-level `purchased` row, followed by as many `issued_to_site` rows as there are projects drawing from it (each with its own quantity, and for serialized items like panels, its own slice of `serial_numbers`). `material_stock`/`material_requirement`/`material_shortfall` already aggregate correctly across any number of projects.
- **Financial Obligations is five tables, not one** (0006): `financial_obligations` (the planned/quoted layer), `sales_invoices`/`sales_invoice_items` (what's actually billed — note goods and installation are taxed at different GST rates and typically appear as separate invoice lines), `payment_receipts` (what's actually received, with the same suggested/confirmed matching pattern as everywhere else since most UPI screenshots carry no invoice reference), and `tally_ledger_entries` for reconciling against an uploaded Tally export. Quoted/Invoiced/Received/Pending are all computed via `financial_obligation_reconciliation`/`project_financial_summary`, never stored.
- **Project_Milestones covers four tracks**, not the DISCOM subsidy funnel — that's confirmed out of scope since it pays the customer directly. Each track (`geda_registration`, `discom_feasibility`, `ceig_inspection`, `site_installation`) has its own milestone vocabulary that differs by `project_type`; don't hardcode one linear sequence.
- **BOM generation for standard systems will eventually be sheet-driven, not app-code-driven** (0007): NRG already has a private Google Sheet (the "SPP Bill of Material") that correctly calculates quantities from driver inputs (panel count, rows, frame length, structure height, legs per frame, string count). NRG SolarConnect's job is to write those inputs to the sheet and read calculated quantities back — **never reimplement the sheet's formulas in application code**. `boms.origin` (`uploaded_workbook` vs `system_calculated`) and `boms.generation_inputs` (jsonb) exist now so a calculated BOM can be told apart from an uploaded one, but the actual Sheets API input/output cell contract is still unmapped — don't build the integration itself yet, that mapping is explicitly deferred to a later design session.
- **Rates for a system-calculated BOM come from Tally purchase history, never from any static rate sheet** — reuse `material_weighted_avg_rate`/`material_purchase_running_avg` (0005). NRG is connecting Tally live via MCP; `tally_ledger_entries.sync_source` (0007) distinguishes a live-synced row (`tally_live_sync`, no document) from an uploaded export (`manual_upload`, the 0006 default). Once live sync exists, expect most new `tally_ledger_entries` rows to have `source_document_id null`.
- **Stock statements are frozen, editable snapshots — never regenerate one from a live query** (0007). `stock_statements`/`stock_statement_items` capture `book_quantity` (system-computed, point-in-time via the new `material_stock_as_of(date)` function — **not** the live `material_stock` view, which would silently pull in later transactions), `physical_quantity` (from the monthly physical count), and `declared_quantity` (the actual number submitted to the bank — always manually editable/overridable with a note, since physical counts don't always match the system and someone has to sign off on the final figure). Valuation uses the new `material_weighted_avg_rate_as_of(material_id, date)` function, same LATERAL-join machinery as `material_transaction_cost` (0005), just point-in-time. `materials.stock_group` is a coarse reporting rollup — deliberately separate from `materials.category` (the ~15 fine-grained BOM engineering categories); don't conflate the two. Confirmed real values (stayed free text, no migration needed to set these): `dcr_panels` | `ndcr_panels` | `inverters` | `structure` | `acdb_dcdb` | `cables` | `cabling_accessories` | `hardware` | `misc` | `retail_non_project`. DCR/NDCR panels are split because only DCR qualifies for subsidy eligibility and a bank statement needs that split visible without filtering; Cables and Cabling Accessories are split too. `stock_statement_summary` view supports month-over-month comparison directly (filter by `stock_group` and two `statement_period` values).
- **Not every sale is a "project."** NRG also stocks and sells Havells heat pumps, solar dryers, solar water heaters — retail off existing stock, never installed/tracked as a project (no milestones, no BOM, no `financial_obligations` funnel). `material_transactions.movement_type` now includes `'sold_direct'` (0008) for this — always `project_id`/`bom_item_id` null — and links back to exactly which invoice line it was sold against via the new `sales_invoice_item_id` column. `sales_invoices.project_id` is now nullable with a new `customer_id` column for this case (`check (project_id is not null or customer_id is not null)`); project-linked invoices are unaffected. Different sized models (200L/300L heat pump, etc.) are just ordinary new `materials` rows — same AI-extraction-off-a-scanned-invoice flow as everything else, no special handling.
- **`document_type` is now a settled, complete reference list (0009/Section 36)**, grouped into sales & compliance, GEDA/CEIG/DISCOM chain, engineering drawings, inventory/material movement, financial reconciliation, KYC/compliance checklist, and visual. Two direction-ambiguous types were split rather than given a schema column: `purchase_order_customer` vs. `purchase_order_vendor` (same document label, opposite direction — Section 24). `payment_proof` covers receipt book/cheque photo/UPI screenshot/bank excerpt as one type, since the method is captured on `payment_receipts.payment_method`, not the document. **Document type/template alone cannot reliably determine movement direction** for `material_out`/`material_in`/`delivery_challan`/`gate_pass` — a Gate Pass pad is used for both directions, disambiguated only by a handwritten note — extraction has to read content, not trust the label. The KYC types (`pan_card`, `aadhaar_card`, `gst_certificate`, etc.) populate nothing beyond `documents` itself; they're checked off against a `required_documents` reference list per `project_type` at the app layer — no new table, that's a query against what's already uploaded, not stored state.
- **`electrical_test_records` (0009)** is a new dedicated table for the CEIG Test Inspection Application + Inspection Report pair — genuinely measurable, fixed-shape facts (Megger readings, earth pit resistance across typically 4 pits, contractor/supervisor license numbers), not generic AI Facts rows. `file_no` links the two documents (`application_document_id`/`report_document_id`). `status = 'satisfactory'` is the trigger event for the CEIG track's completion milestone in `project_milestones` — same role the GEDA Registration Letter plays for `geda_registration`. `issuing_authority` is free text on purpose — approval authority is regional/scale-dependent, not always the Chief Electrical Inspector.
- **`documents.project_id` is now nullable (0009)** — it was `not null` since it was first drafted, which silently made it impossible to store a warehouse-level purchase document or a direct-sale invoice (both legitimately projectless). Fixed to match `sales_invoices.project_id`'s already-nullable shape.
- **New `material_transactions` columns (0009)**: `dcr_ndcr_status` (Domestic Content Requirement status per purchase batch — affects government-scheme eligibility), `batch_ref_no` (NRG's own fiscal-year + type-letter + sequence identifier, e.g. `18-19-P-01`, matching their existing internal procurement logs), and a self-referencing `replaces_transaction_id` for warranty replacements — a real case exists where a new serial supersedes a failed one against the same project.
- **A known architectural gap you should flag to the design chat**: this session's `profiles`/auth table and the design chat's `employees` table haven't been reconciled — decide whether they're the same entity (`employees.employee_id = auth.users.id`) or `employees` is a superset with an optional `auth_user_id`, before building anything that depends on "who is this person."
- **Delivery Challans are now generated, not just scanned (0010) — this is the highest-priority piece of everything in this handover.** Opening "generate DC" for a project should pre-fill line items from `bom_items.planned_quantity` minus what's already gone out net of returns — never a blank form. The generated PDF becomes the `documents` row (`delivery_challans.generated_document_id`) and the `material_transactions` `issued_to_site` rows (`delivery_challan_id` set) simultaneously — there's no separate extraction step for a system-generated DC, only for the rare handwritten paper-book fallback (which leaves `delivery_challan_id` null). `document_number_sequences`/`next_document_number()` gives controlled, gapless-per-series numbering — reuse it if/when PO generation happens later, don't build a second numbering mechanism.
- **Commissioning Report generation is two-sided, not one (0010)** — unlike the DC, NRG's real commissioning report is a **prefilled printed template that gets handwritten readings added on-site, then re-uploaded**. So `commissioning_reports` needs both a generation flow (produce `template_document_id`) and an extraction flow (read `filled_document_id` back into the structured fields) — don't build only one half. A confirmed report should flip `projects.status` to `'commissioned'`.
- **PO generation is confirmed not critical** — most vendor POs are just phone calls today. Don't prioritize building a PO-generation UI; `purchase_orders` (0003) already works fine for logging POs after the fact.
- **`project_costs` (0011) is capture-only, not a margin report.** Vendor/transport bills usually don't mention a project, so they land as `assignment_status = 'unassigned'` and MP assigns them by hand — same pending-until-resolved pattern as `payment_receipts.match_status`. `'general'` is a real, distinct outcome (e.g. one transport bill covering three residential dispatches on one truck) — never collapse it with `'unassigned'`; one means "not looked at yet," the other means "looked at, correctly belongs to no single project." **Do not build a margin/profitability view against this yet** — margin is explicitly still being defined in the design chat; this migration only makes sure the cost data exists and is properly attributed before that conversation happens.
- **`customers.aliases` (0012)** — same pattern as `materials.aliases`. Real case: an electricity bill shows the legal owner's name while the whole team refers to the project by its business name. `name` stays the primary/display name; `aliases` holds every other variant, and both should be checked when matching an incoming document to a customer.
- **Compliance milestone vocabulary is richer than what shipped so far — no schema change, just use the fuller list.** `geda_registration`: `application_submitted` → `fees_paid` → `registered`. `discom_feasibility`: DISCOM's SR No. goes in `project_external_references` (`reference_type = 'discom_sr_no'`), then `estimate_paid` → `feasibility_generated`. `ceig_inspection`: five steps confirmed, not three — `applied` → `sld_approved` → `test_inspection_applied` → `satisfactory_letter_received` → `certificate_received` (the satisfactory letter and the final certificate are two separate documents/events).
- **Fabricator/electrician per project already has a home — `project_milestones.vendor_id`.** Populate it on the relevant `site_installation` steps (structure/panel-fitting steps → the fabricator partner; inverter/ACDB-DCDB/earthing steps → the electrician partner) rather than adding a new column. Surface it on the Project Overview as a plain "Team" line so nobody has to dig through individual milestone rows to find it.
- **The financial visibility boundary is narrower than first stated — corrected in Section 39.** The obligation ledger (`financial_obligations`/`sales_invoices`/`payment_receipts` reconciliation — Quoted/Invoiced/Received/Pending) is visible to **everyone** (Sales, Sales-backend, Service, Purchase, Owner) — it's operationally necessary, e.g. MP checking payment status before releasing a dispatch. Only cost/margin is owner-only: `bom_item_variance.actual_cost`/`cost_variance` specifically (quantities on the same BOM rows stay visible to everyone), `project_costs` in aggregate, and any future margin view. A bill in `project_costs` is still visible to MP while they're actually assigning it — the restriction is on the aggregated cost view, not on someone doing their job. **This applies to menus too, not just data**: a report/tab a role can't see should be absent from the UI entirely, not shown locked/greyed — don't announce that something exists if the role can't have it.
- **Vendor quote rates aren't always comparable as raw numbers (0013).** `includes_transport`/`includes_loading_unloading`/`notes` on `vendor_quote_items` — null on all three means "not specified" (a common, legitimate case for a phone-quoted rate), not missing data. The comparison screen should show these flags next to the rate rather than silently ranking cheapest-first as if every quote were on equal terms.
- **`materials.make` (0013) — a real, named gap: Sales was quoting Waree 620W while Adani 620W sat in stock.** The old `unique(category, canonical_name)` constraint is now `unique(category, canonical_name, make)` — two brands at the same wattage are two distinct materials rows. Also a permissions note: Sales needs read access to stock levels by material even though they do almost no document processing in NRG SolarConnect itself.
- **Capacity Consistency Check needs a tolerance band, not exact-match** — quoted vs. as-purchased wattage naturally drifts a little (manufacturing "Wp binning," e.g. quoted 620W, received 615W) and that's not an error; disagreement *across government documents* (Quote/GEDA/Feasibility/Registration) should still be flagged at zero tolerance. Report logic only, no schema.
- **The approval threshold is ₹25,000, confirmed real (0015).** `quote_comparison_requests.total_amount` is snapshotted at submission, not recomputed live — so the number the owner gets notified about can't drift if quotes change after the fact. Above ₹25,000, notify the owner on `draft → submitted`; below it, MP can self-clear. `submitted → approved` notifies MP a PO can be raised. Both are app-layer jobs, same pattern as `reorder_alerts` — don't build DB triggers for this, the state transition is all the DB provides.
- **Vendor rates need a landed-cost estimate, not just a yes/no flag (0015).** When `includes_transport`/`includes_loading_unloading` (0013) is false, MP fills in `vendor_quote_items.estimated_transport_charge`/`estimated_loading_unloading_charge` so the comparison can rank by effective landed cost (`rate × qty + both estimates, nulls as zero`), not the printed rate — otherwise the vendor who excluded the most from their number always looks cheapest.
- **`partners.supplies_categories` (0015) is a manual tag, not a substitute for real history.** Only useful for a vendor with no order history yet — a vendor who's actually been quoted or bought from should be found by joining `vendor_quote_items`/`material_transactions`, not this array.
- **`employees` (0002) already covers the editable People roster** — name/role/phone/email/active is exactly what an Owner/MD, Maintenance, Engineer, Accounts, Purchase list needs. When someone leaves: update their row with the replacement's details, or set `active = false`. **Never delete an employee row** — `confirmed_by`/`sales_person_id`/`assigned_by`/etc. across this entire schema point at `employee_id`, and a delete orphans that history.
- **"Order Now" is now a real view column, `material_shortfall.order_now` (0016) — read it, don't recompute it in the UI.** Confirmed formula: `current_stock + open_order_quantity + reorder_level − outstanding_requirement`. Negative means order it. The existing `shortfall` column stays too (a different, still-useful question — requirement vs. supply with no safety buffer folded in); don't conflate the two. The PO lifecycle already zeroes `order_now` out correctly with nothing new to build — `material_open_orders` (0003) already nets ordered quantity against what's actually been received.
- **A sheet-generated BOM needs two sign-offs before it drives ordering, not one (0016).** Engineer confirms first (existing `extraction_status`/`confirmed_by`/`confirmed_at`), then `boms.owner_approved_by`/`owner_approved_at` is a separate second tick. `material_requirement` now only counts a BOM's `planned_quantity` once `extraction_status = 'confirmed'` **and** (`origin = 'uploaded_workbook'` **or** `owner_approved_at is not null`). The carve-out matters: an uploaded workbook is already a human-authored document and only needs the one existing confirmation — don't require owner approval on those too, that would silently zero out every historical BOM's contribution to ordering the moment this ships. The second tick is only for `origin in ('system_calculated','recommendation_engine')`.
- **Three scheduled digest emails, confirmed requirements — app-layer cron jobs, no new schema.** Each is one digest email, not one per row: **What Needs Ordering** (`material_shortfall` where `order_now < 0`) → `purchase@nrgtechnologists.com` + owner, daily 8:00am. **Stock Statement** (`stock_statement_summary`, latest period) → purchase + owner, weekly Saturday 3:00pm. **Required Documents Gap** → `sales@nrgtechnologists.com` + `service@nrgtechnologists.com` + owner, daily 8:00am. Same pattern as `reorder_alerts` — the views already answer these live, this is a scheduling/templating task.
- **Quote comparison needs an approval gate before it can become a PO (0014).** `vendor_quotes.is_selected`/`purchase_order_id` (0004) technically already let a quote convert straight to a PO — the missing piece was ever requiring a sign-off first. `quote_comparison_requests` batches 2-4 vendor quotes (one or several materials) under one decision: MP builds it in `'draft'`, flips to `'submitted'`, owner sets `'approved'`/`'rejected'` with `decision_notes`. Enforce at the application layer: only an `'approved'` request should let the app set `is_selected` or create a `purchase_orders` row. Sending the comparison for approval (email/WhatsApp/push) is your job to build — the DB only tracks state.
- **Vendors are `partners` with `category = 'vendor'` — don't build a separate vendors table.** `partners.contact_person` (0014) is the one field that was missing (name/phone/email/GSTIN/address already existed since 0002).
- **`stock_statement_summary` was missing `materials.make` (0014)** — recreated via `create or replace view` since `make` (0013) landed after this view was first written. A real bank statement needs Adani 545W and Waree 545W as separate lines, not folded into one panel total.
- **Cross-checked the real SPP Bill of Material sheet — `materials.make` already covers it, no schema gap.** Confirmed real panel brands across ~30 project tabs: Adani, Waree, Goldi (wattages 540-725 depending on vintage). Confirmed real inverter brands: Growatt (dominant), Goodwe. Useful for seed/demo data — a `materials` row should never be just "Solar Panel," it's always make + wattage as separate rows (e.g. `Adani 615W`, `Waree 615W` are two different `material_id`s, same as `Adani 545W` vs. `Waree 545W` already established in 0013).
- **"Order Now" is a display rule, not a formula change: only show the number when it's negative.** `material_shortfall.order_now` (0016) stays exactly as defined. UI guidance for whatever screen renders it: `order_now < 0` → show the actual (negative) value, that's what needs ordering; `order_now >= 0` → render "Adequate," not the positive number. A positive number under a column called "Order Now" reads like a quantity to buy, which is backwards — it's surplus.
- **Quote Comparison's material list should be filtered to `order_now < 0` only.** Don't show every material MP has ever compared — only the ones actually short right now. Same `material_shortfall` view drives both the Stock dashboard and this filter.
- **`structure_type` is a real driver input for BOM generation (Section 33/45), one more key in `boms.generation_inputs` — no new column.** Value `'rcc'` or `'sloping_roof'`. RCC needs the full GI-pipe-frame input set (`frame_length_ft`, `legs_per_frame` — Section 33's existing list); Sloping Roof uses Aluminium Strut Channel instead and doesn't need those two fields at all. The two structure material families are just different `bom_items` rows the sheet returns, not a schema branch.
- **The SPP sheet round-trip contract Section 33 deferred is now settled: duplicate-tab-per-generation-run, not writes against one shared live tab.** Duplicate the `Template` tab via the Sheets API per BOM generation, write driver inputs into it, let the sheet's own formulas recalc, read **only** the `Qty Required` column back out as `bom_items` (`extraction_status = 'ai_extracted'`, `origin = 'system_calculated'`), then the duplicate can be discarded. Per-run duplication matters specifically so two engineers generating BOMs at the same time can't clobber each other's inputs/outputs on a shared tab. **Do not read the sheet's own rate/price column** — `bom_items.planned_rate` for a system-calculated BOM comes from `material_weighted_avg_rate_as_of()` (same as the line above), never from the sheet. No schema impact — pure integration detail, see Section 45 for the full write-up.
- **`generated_documents_log` (0017) is a `union all` view, not a shared table — build the "everything we've generated" list screen against it directly.** Covers `delivery_challans`, `purchase_orders`, `commissioning_reports`, `boms`. Each row's `document_type` + `document_id` is what you deep-link with; don't try to join back through the view itself for detail, query the underlying table. `related_to` is the vendor name for POs specifically (they're not project-scoped) and the project/customer name for the other three — don't assume it's always a project.
- **New Material / New Supplier / New Installation Team forms need zero schema work — confirmed, don't go looking for missing tables.** New Material → insert into `materials`. New Supplier → insert into `partners` with `category = 'vendor'`. New Installation Team → also `partners`, just `category = 'fabricator'`/`'electrician'`/`'civil_contractor'`/etc. (free text, Section 2) — it's the same table `project_milestones.vendor_id` already points at for fabricator/electrician assignment (Section 39). All three forms are pure UI over existing shapes.
- **Bank Reconciliation (0018) is a different reconciliation than `tally_ledger_entries` already does — don't conflate them.** The existing one (Section 32) is Tally vs. NRG's own obligations/receipts, project-facing. This one is Tally vs. the actual bank statement, project-independent, accounts-facing. `bank_statement_transactions` follows the exact same extract-then-match pattern as `payment_receipts`/`tally_ledger_entries` — AI suggests a match by amount/date/reference, a human confirms, never auto-applied. `bank_reconciliation_gaps` is the screen itself: everything unmatched/discrepant on either side, `union all`'d into one list, confirmed-matched rows excluded entirely. Access control (Accounts-role-specific) isn't decided yet — real bank data, so don't default it to everyone-can-see the way the obligation ledger is; treat it like the still-pending roles/permissions item until that design happens.
- **Navigation should be role-based, not one screen with every action stacked regardless of who's looking (Section 48) — no schema impact, pure UI/IA.** Concrete rule: Quote Comparison/Vendor Master/Raise PO are Purchase's; Installation Teams & Contractors is Engineer's, not Purchase's, even though it sits next to Vendor Master in the mockup's Stock screen; Generate DC is Dispatch's, Generate BOM is Engineer's, Commissioning Report is Service's, Generate PO is Purchase's — don't build one shared "Generate" menu showing all four to everyone. Basic Stock (the category-grouped material view) is the one screen both Purchase and Engineer genuinely need, so it's the exception that stays shared. Full role → screen table is in Section 48 of the handover doc. This is a navigation-design decision now; real enforcement still waits on the roles/permissions + RLS work already on this list.
- **Quote Comparison's real filter is `order_now < 0 OR current_stock < reorder_level`, not just the first one.** Urgent (red) vs. below-reorder-level-but-not-urgent (amber) are both real, distinct signals worth showing — Section 49. Same `material_shortfall` view drives both, just two conditions instead of one.
- **`vendor_inquiries` (0019) has no delivery/read status — don't build one.** `wa.me` click-to-chat gives no callback at all; the table only claims "this was sent," not "delivered" or "read." One row per vendor per send (checking 5 vendors = 5 rows), never a batch row — each is a genuinely separate WhatsApp conversation. True bulk-send needs the paid WhatsApp Business API; out of scope, don't build toward it without being asked.
- **KP and Engineer are the same role — don't model them as two.** Whoever handles GEDA/CEIG/DISCOM paperwork (KP, the Pending board's compliance track, Section 16) is the same person who runs BOM generation (Engineer, Sections 33/43/48). One person at NRG's scale wears both hats.
- **Generate DC is multi-role: Purchase, Dispatch, or Service can all end up doing a delivery run.** Don't lock DC generation to a single "dispatch" role/permission — Section 51. BOM stays Engineer-only, Commissioning Report stays Service-only, PO stays Purchase-only; those genuinely are single-role.
- **Reports access settled as three roles — Team (default) / Accounts / Owner — corrected down from an earlier five-role draft (Section 52).** What Needs Ordering, Stock Statement History, and Bank Reconciliation → Accounts + Owner only. Everything else (Milestones, Capacity Consistency Check, Required Documents Gap, Generated Documents) → everyone, no restriction. Margin → Owner only, unchanged. Purchase deliberately has no dedicated Reports pill — they already get What Needs Ordering/Below Reorder/Order Now on their own Stock screen (Section 49); don't duplicate it under a Purchase-specific Reports view. Still UI-only until real RLS exists.
- **DC generation isn't capped at `bom_items.planned_quantity` anywhere, and was never meant to be — Section 53.** A DC can legitimately exceed 100% of planned quantity (a real on-site shortfall found after full dispatch) or be generated with no Pending item involved at all (Project page now has its own "+ Log Dispatch"/"+ Log Return" buttons). Don't add a cap; the "X left of Y planned" figure is a UI hint, never an enforced ceiling.
- **Material Return is its own flow (Section 53), reusing the existing `returned_to_warehouse` movement type — no new column there.** What's new is only the generation screen and a lightweight "Material Return Note" document, matching the DC pattern. `material_transactions.source_document_id`'s own comment already anticipated this document type since 0002 — it just never got built.
- **Real bug fixed in 0020: `material_stock` never actually subtracted `sold_direct`.** 0008 added the movement type and claimed in a comment that the view already handled it — it didn't. Every direct sale has been overstating `current_stock` since 0008 shipped. Fixed as part of the same `create or replace view` that adds `stock_adjustment`.
- **Monthly stock adjustments are a new, deliberately signed movement type (`stock_adjustment`, 0020) — the one exception to the all-positive-quantity convention.** `stock_adjustments` records book vs. counted vs. reason vs. who; `stock_adjustment_summary` is the actual report, with a ₹ value on the variance via `material_last_purchase.rate`. Written in one action, no draft/confirm step — a physical count doesn't have an AI confidence score to review.
- **Legacy/untracked stock needs zero new schema — it's an ordinary `materials` row + one `'purchased'` transaction, with a system-generated "Legacy Stock Entry" document standing in for the missing original invoice (Section 54).** Same pattern as Delivery Challans/Material Return Notes satisfying `source_document_id not null` with no real external paper trail. Once that transaction exists it flows into `stock_statement_summary` like anything else — same statement the bank sees, no second spreadsheet.
- **DC:AC ratio (`boms.inverter_capacity_kw`, `bom_capacity_check` view, 0021) flags oversizing past 1.4×, same pattern as `kw_capacity`.** One header-level number for total inverter capacity, not derived from summing `bom_items` (capacity isn't reliably parseable out of a material name). Confirmed real threshold: 1.4. Show it live while a BOM is being generated, not just after the fact on Reports — catching the mistake at entry time is the actual point.
- **`project_costs` bills are confirmed multi-line, each line naming its own project (0022) — not an undifferentiated lump sum needing manual guesswork.** One bill, N `project_costs` rows sharing one `source_document_id`, was already right (Section 38). New: `assignment_status` gains a `'suggested'` state + `ai_confidence`, so AI can fuzzy-match each line's stated project name (same `customers.aliases` pattern as everywhere else) and MP just confirms, instead of every line starting blank.
- **Client Contacts (Owner/Client, Maintenance, Office, Accounts, each with Call/WhatsApp) needs zero schema — it's `customer_contacts`, already fully shaped for this (Section 58).** Distinct from the company-wide People tab (`employees`, Section 43) — this is the client's own contacts, not NRG's staff. "Office" is just a new free-text `role` value.
- **The BOM Recommendation Engine (0013) is done — the last big item on the original Next Session list.** `material_consumption_benchmark` computes `used_qty_per_kw` per material from **`commissioned`** projects only (a still-dispatching project would skew the benchmark low) using real `bom_item_variance.used_quantity` (dispatched minus returned), not planned quantity — that's the entire point, learning from what actually happened on site, not what was estimated. `project_sample_size` should drive the recommendation's own confidence — 1-2 historical projects is a guess, 10+ is a real pattern. A generated BOM from this is ordinary `bom_items` rows with `boms.origin = 'recommendation_engine'` (a new third value alongside Section 33's `'system_calculated'`), reviewed and confirmed like everything else — never auto-applied. Coexists with, doesn't replace, Section 33's sheet-driven path.

## Still pending in the design chat

The document processing workflow itself (trigger → classify → extract → write, retry/failure handling, human-review queue for low-confidence extractions — the taxonomy this workflow runs against is now settled, see Section 36), user roles/permissions + Row-Level Security policies (design these before real users touch the app — retrofitting RLS later is expensive; the financial visibility boundary above is already decided, RLS just needs to enforce it at the database level, not just hide it in the UI), an audit log for manual edits (separate from the AI extraction_status pattern already in place), BOM Recommendation Engine, Marketing module, margin/profitability reporting (deferred until margin is properly defined — see `project_costs` above), a report aggregating `project_milestone_durations` across all projects to find systemic bottlenecks (no new schema needed, just a cross-project view), and a suggested-reorder-level mechanism (compute from consumption rate × lead time, same `ai_extracted`/`confirmed` pattern as everything else, rather than pure manual entry). Also worth planning for, not urgent yet: soft-deletes/retention policy for financial and KYC documents (Aadhaar/PAN cards are in scope as Documents — this has real Aadhaar Act / DPDP Act implications, not just a nice-to-have), and idempotency in the AI extraction pipeline so a retried/duplicate document upload can't create duplicate `bom_items`/`material_transactions` rows.
